# 云梯 UX Contract

## 账户与中国网络访问

- 浏览器只通过当前云梯域名下的 `/api/auth/*` 登录、注册、退出和读取会话，不直接请求 `*.supabase.co`，也不持有可供 JavaScript 读取的 Supabase 访问令牌。
- Supabase 会话由服务端写入 HttpOnly Cookie。受保护 API 必须在服务端验证会话，再将已验证用户标识传给持久化层；客户端提交的身份标识不得作为权限依据。
- 普通页面首屏不在中间件中跨境校验账号。未登录页面只加载登录所需代码；完整课程生成器在会话确认后再加载。
- `/` 是轻量登录入口；会话确认后进入 `/workspace`。未登录用户即使直接访问 `/workspace`，也必须先通过同一账户边界。
- 会话确认最多等待 12 秒。超时或服务异常时显示可恢复的登录错误，不允许无限加载，也不因认证不可达而误放行受保护功能。
- `onlineteacher.bookln.cn` 统一跳转到 `www.onlineteacher.bookln.cn`，减少两个入口产生的缓存和会话分裂。

## 单页互动内容生成

- 首页的核心产出固定为一页，类型只有 3D 可视化、互动模拟、教学游戏、思维导图和测验。
- 用户显式选择页面类型后，客户端和 MCP 直接构建单页大纲，不再调用整课大纲模型。
- 该路径只调用一次内容模型，不生成智能体角色、教师台词、语音或视频；图片仅在用户显式开启时使用。
- 30 秒是性能目标而非硬超时；上游在 30 秒时仍在返回有效内容时不应为了速度发布错误类型的降级页。

## 公开课程观看

- `/classroom/:id` 是可分享的匿名观看链接，不要求登录或访问码，也不展示登录入口、账号角标、顶栏、侧栏、聊天、白板和编辑工具。
- 页面使用动态视口高度 `100dvh`，课件按原始 16:9 比例完整显示；多余区域使用纯黑安全区，横竖屏均不得产生页面滚动。
- 浏览器禁止未经手势的有声自动播放，因此首屏保留一个居中的轻触播放提示。用户轻触画面后开始课程和语音，该提示随即消失。
- 普通讲授页播放结束后自动进入下一页；互动页不自动跳过。手机用户可左右滑动切换场景，桌面用户仍可使用方向键。
- 互动页在不超过 768px 的窄屏中，启动、暂停、继续和重置等主要实验操作必须始终可达。观看端将课件原按钮移动到底部安全区操作栏并保留原事件；切回宽屏后恢复到课件原位置。
- 课程结构通过只读公开接口加载。生成、保存、聊天、设置和模型接口继续要求登录，服务端 Key 不下发到观看端。
- 旧课程缺少音轨时，语音按当前固定课程文案逐条生成并持久化，不能允许观看端提交任意 TTS 文本。

## 状态与恢复

- 加载中：黑色全屏底，仅显示“正在加载课程…”。
- 加载失败：黑色全屏底，显示简短错误说明和一个“重新加载”操作。
- 音频首次生成失败：课程画面仍可继续，播放引擎按文本时长推进；下一次播放可重试生成。
- 刷新和重复访问：始终读取服务端课程真源，不让旧的浏览器缓存覆盖新生成的音轨。

## 测验批改与保存

- 只有选择题的测验在用户提交时立即在本地计算并显示成绩，不显示 AI 批改等待页。
- 简答题可在远程批改期间显示等待状态；批改完成后先显示结果，再在后台持久化。
- 草稿写入按最新答案合并。完整的“已提交”或“已批改”快照覆盖尚未执行的中间草稿，不得为了顺序写入每道题而阻塞成绩展示。
- 未登录的分享课程用户仍可本地作答和批改；“需要登录才能持久化”只使远程保存降级，不得把测验替换成错误页或要求观看者登录。

## 生成内容的手机布局

- 单页互动内容在生成阶段必须同时满足 320px 和 390px 宽度：无水平页面滚动、无文字裁切、无节点或按钮重叠。
- 思维导图在窄屏不缩放桌面节点图，而是改为正常文档流中的单列层级卡片；节点自动增高、文字换行，手机端可简化连线。
- 3D 可视化、互动模拟和教学游戏在手机端将控件置于画布上方并自动换行，画布使用容器尺寸重算，不用整页等比缩放代替响应式重排。
